# Disabled tools that depend on Boas Dialect
# add_subdirectory(boas-opt)
# add_subdirectory(boas-compile)

# Standalone tools that work independently
add_subdirectory(boas-run)
add_subdirectory(standalone-conversion-test)
