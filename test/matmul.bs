import tensor
from tensor import matmul

def main():
    # sdad
    A = tensor([[0, 0],
                       [4, 5]])
    
    B = tensor([[2, 2],
                       [0, 4]])
    
    C = tensor.matmul(A, B)
    
    print(C)
